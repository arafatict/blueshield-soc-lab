[project]
name = "blueshield-soc-lab"
version = "0.1.0"
description = "A defensive SOC learning lab using synthetic Windows security telemetry"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0"]
[project.scripts]
blueshield = "blueshield.cli:main"
[tool.pytest.ini_options]
pythonpath = ["src"]
