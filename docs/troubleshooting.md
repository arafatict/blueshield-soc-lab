# Troubleshooting

Use Python 3.11+, install dependencies with `python -m pip install -e .`, and run tests with `pytest -q`. If a scenario is not recognized, use `blueshield generate --help`. Remove generated output directories before repeating a run.
