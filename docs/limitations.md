# Limitations

This is a reproducible defensive learning lab, not a production SIEM or SOC. It does not collect real Windows telemetry, provide authentication, store multi-user cases, or execute suspicious commands. All events are explicitly marked as simulated.
